# Puerto en el que corre el servidor (opcional, por defecto 3000).
PORT=3000

# Todo lo demas (QuickBooks, Google Maps, choferes, negocio, reglas de zona)
# se configura desde el panel web en /admin, con formularios, no hace falta
# editar archivos para eso.
